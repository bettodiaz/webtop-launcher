# Webtop Launcher

## Quick Start
bash build_webtop_launcher.sh
cd webtop-launcher
docker compose up -d

Open your browser to localhost (port 80).
Backend API runs on port 8080.

Default credentials:
- Username: admin
- Password: admin123
